# Current mathematical and experimental manuscript

The main paper has 14 numbered equations, 44 cited references, seven figures and three tables. The comprehensive Figure 2 traces the evidence, scoring and experimental paths. Figure 5 includes full component ranks, a 3D rank-response view at discrete radii, and exact first-priority weight intervals. The supplement contains full supporting results and the new interval table (S13).

The new decision-weight analysis uses recorded fixed E2024 component summaries. It adds deterministic calculations and visualisation; it does not create new field observations or alter earlier experiments. The 500 m exact top-three probabilities agree with the recorded 50,000-draw uniform-weight experiment within 0.002382. Internal review history is retained in repository documentation outside the article.

The supplied Figure 1 is unchanged. All new figures are editable vectors with Python builders. Run `scripts/build_decision_geometry.py`, `scripts/build_framework_figure.py`, then `scripts/build_paper_experiments.py` in the full repository to regenerate their inputs/outputs. The manuscript source archive supports LaTeX compilation; rerunning figure builders requires the full repository's derived-data package.

Author declarations and reviewer-access arrangements remain pending. This package is for author review before journal submission.
